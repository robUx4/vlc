From b06bd27fa444a0c8d61fa8e93a711af96d594d21 Mon Sep 17 00:00:00 2001
From: Steve Lhomme <slhomme@matroska.org>
Date: Wed, 24 Jun 2015 10:26:38 +0200
Subject: [PATCH 2/3] fix compilation with MSVC

---
 libarchive/archive_write_set_format_pax.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/libarchive/archive_write_set_format_pax.c b/libarchive/archive_write_set_format_pax.c
index 687f8e4..fe3ba77 100644
--- a/libarchive/archive_write_set_format_pax.c
+++ b/libarchive/archive_write_set_format_pax.c
@@ -1563,7 +1563,7 @@ build_pax_attribute_name(char *dest, const char *src)
 	 * recomputing it every time.  That will also open the door
 	 * to having clients override it.
 	 */
-#if HAVE_GETPID && 0  /* Disable this for now; see above comment. */
+#if 0  /* HAVE_GETPID - Disable this for now; see above comment. */
 	sprintf(buff, "PaxHeader.%d", getpid());
 #else
 	/* If the platform can't fetch the pid, don't include it. */
@@ -1612,7 +1612,7 @@ build_gnu_sparse_name(char *dest, const char *src)
 		break;
 	}
 
-#if HAVE_GETPID && 0  /* Disable this as pax attribute name. */
+#if 0  /* HAVE_GETPID - Disable this as pax attribute name. */
 	sprintf(buff, "GNUSparseFile.%d", getpid());
 #else
 	/* If the platform can't fetch the pid, don't include it. */
-- 
1.9.5.msysgit.0

